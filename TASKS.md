# TASKS.md: SADA (Semantic Attribute-Driven Anchoring) Implementation Plan

## Project Overview
This project extends the "VtT" model, a fine-tuning method for CLIP in Source-Free Cross-Domain Few-Shot Learning (SF-CDFSL), to implement "SADA (LLM-driven Semantic Attribute Anchoring & Orthogonal Decomposition)".
The goal is to use multiple semantic attributes generated by a local LLM to separate domain noise from the visual encoder and align pure semantic information with the text space.

## Directory Structure (Actual Codebase)
The following mapping applies between the conceptual SADA components and the real files:

| Component | Actual File |
|---|---|
| Main training loop | `main.py` + `VtT.py:run_lora()` |
| Model definition (Mamba_Net, TIA) | `VtT.py` |
| CLIP text/vision encoder | `clip/model.py` |
| Loss computation | `VtT.py` (inline in `run_lora()`) |
| Argument parser | `run_utils.py:get_arguments()` |
| Attribute generation script | `scripts/generate_attributes.py` (new) |
| Attribute JSON cache | `data/semantic_attributes.json` (new) |

---

## Phase 1: Pre-generation of Semantic Attributes using Hugging Face LLM (Offline Process)
For each class in the target dataset, generate domain-independent semantic attributes using a local LLM via the Hugging Face `transformers` library, and save them as a static file.

- [ ] Create `scripts/generate_attributes.py`.
  - [ ] Use `Qwen/Qwen3.5-9B` via `pipeline("text-generation", ...)`.
    - **Note:** `Qwen/Qwen3.5-9B` is a VLM (image-text-to-text capable), but for text-only attribute generation we use the `"text-generation"` pipeline with text-only messages — no image input needed.
    - Example initialization:
      ```python
      from transformers import pipeline
      pipe = pipeline("text-generation", model="Qwen/Qwen3.5-9B",
                      device_map="auto", torch_dtype=torch.bfloat16)
      ```
  - [ ] Initialize with `device_map="auto"` and `torch_dtype=torch.bfloat16` to fit within a 24GB VRAM GPU environment (e.g., RTX 3090).
  - [ ] Implement a loop to process the specified class list (reuse `label_names` dicts from `VtT.py:run_lora()`).
  - [ ] Format the prompt as a chat message list and pass to the pipeline:
    ```python
    messages = [{"role": "user", "content": f"List 5 distinct, domain-independent visual semantic attributes for the class '{class_name}'. Return ONLY a JSON list of strings."}]
    output = pipe(messages, max_new_tokens=128, temperature=0.1, do_sample=False)
    ```
  - [ ] Set generation parameters to ensure stable JSON output (`temperature=0.1`, `do_sample=False`).
  - [ ] Implement robust JSON parsing (e.g., stripping markdown backticks like ```json if necessary).
  - [ ] Output format: Create a dictionary containing 5 attributes for each class and save it to `data/semantic_attributes.json`.

## Phase 2: Modification of Text Encoder Prompt Initialization
Modify the text branch initialization in `VtT.py:run_lora()` to handle K attribute prompts per class.

- [ ] Modify `VtT.py:run_lora()`.
  - [ ] Add logic to load `data/semantic_attributes.json` at the start of `run_lora()`.
  - [ ] For each class $c$ in `class_texts`, encode its $K$ attribute strings (e.g., $K=5$) through `clip_model.encode_text()` and pre-extract features.
  - [ ] Store the result as `text_attr_features` with shape `(N_classes, K, dim)` and keep it on GPU alongside `text_features`.
  - [ ] This cache is computed once per episode (inside the `for idx, ...` loop) with `torch.no_grad()`.

## Phase 3: Implementation of Orthogonal Feature Decomposition
Add a module to `VtT.py` that separates domain noise and semantic information from the visual features.

- [ ] Add a new `OrthogonalDecomposition` class (`nn.Module`) in `VtT.py`.
  - [ ] Define a single linear projection `proj_semantic: nn.Linear(dim, dim, bias=False)` that acts as the learnable projection matrix $P$.
  - [ ] Compute `V_semantic = proj_semantic(V)`.
  - [ ] Compute `V_domain = V - V_semantic` (guaranteed orthogonal by construction, no additional constraint needed).
  - [ ] **Do not** use two independent projections — that does not enforce $V = V_{semantic} + V_{domain}$ without explicit loss terms.

## Phase 4: Modification of CLIP Text Encoder for Multi-Token Injection
Extend `clip/model.py:encode_text()` to accept K absorber tokens and inject them at K consecutive positions.

- [ ] Modify `clip/model.py:encode_text()`.
  - [ ] Change the `inject` parameter to accept a tensor of shape `(batch, K, dim)` in addition to the current `(batch, dim)` form.
  - [ ] When `inject` has shape `(batch, K, dim)`, inject tokens at positions `5` through `5+K-1`:
    ```python
    x[:, 5:5+K] = inject  # inject K absorber tokens
    ```
  - [ ] Verify that the CLIP context length (77) is not exceeded: `5 + K + len(template_tokens) <= 77`. For K=5 this is safe with the `'a photo of a x.'` template (≈9 tokens).
  - [ ] Keep the existing single-token path (`inject` shape `(batch, dim)`) working for backward compatibility.

## Phase 5: Implementation of the Multi-Head TIA Module
Expand the original Text Encoder Information Absorption (TIA) module in `VtT.py` to produce K absorber tokens.

- [ ] Modify or extend `Mamba_Net` in `VtT.py`.
  - [ ] Add a `MultiHeadTIA` projection head: `nn.Linear(dim, K * dim, bias=False)`.
  - [ ] Accept `V_semantic` (output of `OrthogonalDecomposition`) as input.
  - [ ] Output K absorber tokens `A' ∈ R^(batch, K, dim)` reshaped from the linear projection.
  - [ ] Pass `A'` to the modified `clip_model.encode_text(..., inject=A')` to produce `mae_image_embeddings`.

## Phase 6: Updating Loss Functions
Update the loss computation in `VtT.py:run_lora()` with multi-head $L_{VtT}$ and orthogonal loss.

- [ ] Modify the loss section in `VtT.py:run_lora()`.
  - [ ] **Multi-Head $L_{VtT}$:**
    - Compute cosine similarity between each absorber token $A'^{(i)}_k$ and the corresponding pre-extracted text attribute feature $t_{y_i, k}$ from `text_attr_features`.
    - Formula: $L_{VtT} = -\frac{1}{N}\sum_{i=1}^N \frac{1}{K}\sum_{k=1}^K \cos(A'^{(i)}_k,\ t_{y_i,k})$
    - **Note:** Do NOT use $\cos(V_{semantic}, A'_k)$ — that is self-referential since $A'_k$ is derived from $V_{semantic}$.
  - [ ] **Orthogonal Loss ($L_{ortho}$):**
    - Formula: $L_{ortho} = \frac{1}{N}\sum_{i=1}^N |\cos(V_{semantic}^{(i)},\ V_{domain}^{(i)})|$
    - This serves as a soft regularizer; the architecture already enforces exact orthogonality by construction (Phase 3).
  - [ ] **Combined Loss:**
    - $L_{comb} = L_{ce} + \beta L_{VtT} + \gamma L_{ortho}$
    - Initial value: $\gamma = 0.1$.

## Phase 7: Integration into Training Loop and Gradient Correction
Integrate all changes into `VtT.py:run_lora()` and clarify how ProGrad handles new parameters.

- [ ] Modify `VtT.py:run_lora()`.
  - [ ] Instantiate `OrthogonalDecomposition` and `MultiHeadTIA` alongside `Mamba_Net` and move them to CUDA.
  - [ ] Add their parameters to the optimizer alongside `mamba_params`.
  - [ ] Verify that $L_{ortho}$ is included in the computation graph during backprop.
  - [ ] **ProGrad scope decision:** The current ProGrad loop (`VtT.py:62-95`) targets only `'lora_' in name`. Decide explicitly:
    - **Option A (recommended):** Keep ProGrad on LoRA params only; let `OrthogonalDecomposition` and `MultiHeadTIA` be updated by the full $L_{comb}$ gradient without projection.
    - **Option B:** Extend ProGrad to include all trainable params by removing the `'lora_' in name` filter. Increases compute per step.
  - [ ] Document the chosen option in a comment near the ProGrad call.

## Phase 8: Debugging and Hyperparameter Tuning
- [ ] Add `--gamma` argument to `run_utils.py:get_arguments()` (weight of $L_{ortho}$, default `0.1`).
- [ ] Add `--num_attrs` argument to control $K$ (default `5`).
